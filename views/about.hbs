{{> nav}}

<section class="about">
    <h2>{{about}}</h2>

    <ul class="about__list">
        {{#each technologies}}
        <li class="about__list-item">
            <a href={{this.link}}>{{this.name}}</a>
        </li>
        {{/each}}
    </ul>
</section>